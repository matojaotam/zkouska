[project]
name = "exercise-05-cells"
version = "0.1.0"
description = "Cells analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
